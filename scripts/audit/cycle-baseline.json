{
  "$comment": "FINALIZED runtime import-cycle baseline (DR-4/DR-8, debloat task 010; finalizes task 009's draft). dependency-cruiser is the SOLE acceptance instrument: it counts RUNTIME edges only (the repo config's default tsPreCompilationDeps:false elides `import type` edges; dynamic import() is counted). Each entry is edge-keyed and documents an ACCEPTED, TRACKED runtime cycle back-edge, validated at gate time by scripts/audit/cycle-gate.ts against edgeRegisterSchema (scripts/audit/register-entry-schema.ts). STRICTLY empty here because task 009's measured capture over src reports ZERO remaining runtime cycles after its two extractions (workflow-state-projection<->state-store and core/dispatch<->telemetry/middleware were broken by leaf extraction, not baselined). NO PHANTOM ENTRIES: the gate FAILS CLOSED on any entry that matches no live depcruise-reported cycle edge (the no-mask rule DR-4 leans on hardest), so an entry MUST correspond to a live cycle. The `dispatch -> */composite` seams are NOT cycles (their back-edges are `import type`, elided). The entryShape below is documented for the day a future seam legitimately needs baselining.",
  "version": 1,
  "instrument": "dependency-cruiser (runtime edges: import type excluded, dynamic import counted)",
  "entryShape": {
    "rule": "depcruise rule that flags the edge, e.g. 'no-circular'",
    "from": "repo-relative source module of the back-edge (e.g. src/a.ts)",
    "to": "repo-relative target module of the back-edge",
    "owner": "team/person accountable for retiring the edge",
    "rationale": "why the edge is tolerated for now",
    "issue": "tracking issue for the fix",
    "expires": "ISO date the waiver lapses (XOR 'permanent')",
    "permanent": "true when the edge is an accepted permanent exception (no expiry)"
  },
  "entries": []
}
