---
description: Run a lightweight oneshot workflow — plan + laddered (tier-scaled) implement + optional PR
---

Run a lightweight oneshot workflow — plan, tier-scaled implement, optional PR. Follow `@skills/oneshot/SKILL.md` and `@skills/synthesize/SKILL.md` to handle: $ARGUMENTS
