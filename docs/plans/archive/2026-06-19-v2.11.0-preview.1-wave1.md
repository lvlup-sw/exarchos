# Plan: v2.11.0-preview.1 — Wave 1 (Reliability: #1542, #1307)

> **Design:** [`docs/designs/2026-06-19-v2.11.0-preview.1.md`](../designs/2026-06-19-v2.11.0-preview.1.md) (§4)
> **Workflow:** `v2-11-0-preview-1` (feature) · **Wave:** 1 of 3
> **Branch:** `feature/v2.11.0-preview.1` · ships as one W1 PR to `main`
> **Iron law:** no production code without a failing test first (feature = TDD)
> **Scope:** #1542 (honest delegation readiness) + #1307 (INV-14 merge recovery). W2/W3 plan separately.

## Grounded preconditions (verified this session)

- **#1542:** `PrepareDelegationResult` (`prepare-delegation.ts:122-129`) has `ready`, `readiness`, `blockers?`, `qualityHints?`, `isolation?`, `taskClassifications?` — **no `warnings[]`**. The issue's "schema already has warnings[]" is wrong; adding it is task 1.1. Readiness data (`worktrees.expected` / `worktrees.ready`) is already computed.
- **#1307:** rollback runs `git reset --hard <rollbackSha>` in `execute-merge.ts` (rolled-back result type `execute-merge.ts:94-101` carries `rollbackError?`) and the production adapter `local-git-merge.ts:10`. The git seam is `execFileSync('git',…)` via `gitExec` (`merge-orchestrate.ts:198`), already injectable in tests. `execute-merge.ts:590-595` has a standing INV-14 TODO comment.

---

## Slice A — #1307: INV-14 merge recovery ladder

### Task A1: `recoveryError` discriminator on the rolled-back result
**Phase:** RED → GREEN → REFACTOR

1. [RED] `MergeExecutionResult_RolledBack_CarriesRecoveryErrorDiscriminator`
   - File: `servers/exarchos-mcp/src/verbs/pure/execute-merge.test.ts`
   - Assert the `phase:'rolled-back'` result type accepts `recoveryError?: 'reset-keep-blocked' | 'reset-failed' | 'unexpected-mid-merge-drift'`. Fails: field absent.
2. [GREEN] Add `recoveryError?` to the rolled-back variant (`execute-merge.ts:94-101`), keeping `rollbackError?` temporarily for compat.
3. [REFACTOR] —

**Dependencies:** None · **Parallelizable:** with Slice B

### Task A2: rollback prefers `git merge --abort` (in-merge reversal)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `Rollback_MergeInProgress_InvokesMergeAbortNotHard`
   - File: `execute-merge.test.ts`
   - Inject a `gitExec` stub; drive a failed merge with an in-progress merge state; assert the recovery calls `git merge --abort` and **never** `git reset --hard`. Fails: current code calls `--hard`.
2. [GREEN] In the rollback path (`execute-merge.ts` + adapter `local-git-merge.ts`), attempt `git merge --abort` first when a merge is in progress.
3. [REFACTOR] Extract a small `recoverMerge(gitExec, rollbackSha)` helper encoding the ladder.

**Dependencies:** A1 · **Parallelizable:** No (same files as A3/A4)

### Task A3: post-merge rewind uses `git reset --keep`, never `--hard`
**Phase:** RED → GREEN → REFACTOR

1. [RED] `Rollback_PostMerge_UsesResetKeepNeverHard`
   - File: `execute-merge.test.ts`
   - Assert a post-merge rewind invokes `git reset --keep <rollbackSha>`; assert no `gitExec` call ever contains `--hard`.
2. [GREEN] Replace `git reset --hard <rollbackSha>` with `git reset --keep <rollbackSha>` in `execute-merge.ts` and `local-git-merge.ts`.
3. [REFACTOR] —

**Dependencies:** A2 · **Parallelizable:** No

### Task A4: map failure modes → `recoveryError`
**Phase:** RED → GREEN → REFACTOR

1. [RED] three tests in `execute-merge.test.ts`:
   - `Rollback_ResetKeepRefuses_SetsResetKeepBlocked` (`git reset --keep` exits non-zero because it would discard local work)
   - `Rollback_ResetFails_SetsResetFailed` (reset errors for another reason)
   - `Rollback_MergeAbortLeavesDrift_SetsUnexpectedMidMergeDrift` (post-abort state not at `rollbackSha`)
2. [GREEN] Discriminate the three branches and set `recoveryError` accordingly; surface the worktree as indeterminate.
3. [REFACTOR] Remove the now-stale `rollbackError` field + the `--hard` comments at `execute-merge.ts:7,22,85,100,152,553,590-595` and `local-git-merge.ts:10`; update docstrings to the ladder.

**Dependencies:** A3 · **Parallelizable:** No

---

## Slice B — #1542: honest delegation readiness under native isolation

### Task B1: add `warnings[]` to the output contract (INV-5b)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `PrepareDelegationResult_OutputSchema_AllowsWarnings`
   - File: `servers/exarchos-mcp/src/verbs/team/prepare-delegation.test.ts`
   - Assert the registered output schema for `prepare_delegation` accepts an optional `warnings: string[]`. Fails: field not in interface/schema.
2. [GREEN] Add `readonly warnings?: string[]` to `PrepareDelegationResult` (`prepare-delegation.ts:122`) and to the registered Zod output schema (INV-5b carrier).
3. [REFACTOR] —

**Dependencies:** None · **Parallelizable:** with Slice A

### Task B2: warn when native isolation requested but no worktrees confirmed
**Phase:** RED → GREEN → REFACTOR

1. [RED] two tests in `prepare-delegation.test.ts`:
   - `PrepareDelegation_NativeIsolationZeroWorktreesReady_EmitsWarningKeepsReadyTrue` — `nativeIsolation:true`, `worktrees.expected>0`, `worktrees.ready===0` ⇒ `warnings` contains the shared-checkout caution **and** `ready===true`.
   - `PrepareDelegation_NativeIsolationWorktreesReady_NoWarning` — same but `worktrees.ready>0` ⇒ no such warning.
2. [GREEN] Emit the warning at the native-isolation return path when `nativeIsolation===true && worktrees.expected>0 && worktrees.ready===0`; do **not** change `ready`.
3. [REFACTOR] Factor the message into a named const.

**Dependencies:** B1 · **Parallelizable:** No (same file as B1)

### Task B3: non-native / no-expected paths emit no spurious warning
**Phase:** RED → GREEN

1. [RED] `PrepareDelegation_NonNativeOrNoWorktreesExpected_NoNativeWarning`
   - File: `prepare-delegation.test.ts`
   - `nativeIsolation:false` (or `expected===0`) ⇒ the native warning is absent.
2. [GREEN] Guard the emission precisely on the three-part condition.

**Dependencies:** B2 · **Parallelizable:** No

### Task B4: delegate skill verify-back step (INV-4 regen)
**Phase:** RED → GREEN → REFACTOR

1. [RED] `SkillsGuard_AfterDelegateVerifyBackEdit_StaysGreen` — conceptually, edit the **source** then prove generated output matches.
   - Edit `skills-src/delegate/SKILL.md` (or the delegate command source): add a native-isolation post-dispatch / pre-edit step — "confirm each agent's cwd is under `.worktrees/`; if not, `git worktree add -b <branch> <path> <integration-tip>` and redirect the agent."
2. [GREEN] Run `npm run build:skills` to regenerate `skills/**` + `command-aliases/**`; commit source **and** generated.
3. [REFACTOR] Verify `npm run skills:guard` is green (no drift).

**Dependencies:** None · **Parallelizable:** with Slice A (docs/skill only)

---

## Parallelization

- **Slice A** (`execute-merge.ts`, `local-git-merge.ts`) and **Slice B** (`prepare-delegation.ts`, `skills-src/`) touch disjoint files → parallel-safe.
- Within A: A1→A2→A3→A4 sequential (same files). Within B: B1→B2→B3 sequential; B4 independent.
- **Execution mode:** inline TDD (small wave, 2 focused bugs) — crash-resilient per the inline-vs-subagent learning; commit per slice.

## Verification gates (W1 exit)

- `npm run test:run` (servers/exarchos-mcp) green, including the new RED→GREEN tests.
- `npm run typecheck` clean.
- `npm run skills:guard` green (B4 regeneration committed).
- No `gitExec` path reaches `--hard` (asserted by A2/A3 tests).
- One W1 PR to `main`; CI + CodeRabbit green before merge.

## Coverage map (design §4 → tasks)

| Design requirement | Tasks |
|---|---|
| #1542 `warnings[]` carrier (INV-5b) | B1 |
| #1542 warn-don't-block on native isolation (INV-12) | B2, B3 |
| #1542 playbook verify-back (INV-11, INV-4) | B4 |
| #1307 INV-14 ladder (`--abort`→`--keep`, no `--hard`) | A2, A3 |
| #1307 `recoveryError` discriminator | A1, A4 |
