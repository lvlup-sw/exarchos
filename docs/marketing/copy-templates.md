# Exarchos Marketing Copy Templates

## Positioning Statement (CVFB)

**For** Claude Code power users **who** already use plan.md workflows and need them to survive context loss, scale to agent teams, and actually verify output, **Exarchos is** a free SDLC plugin **that** systematizes design-plan-implement-review-ship into persistent, phase-gated workflows with checkpoint/rehydrate, convergence gates, and exceptional token efficiency. **Unlike** stateless skill bundles or manual markdown conventions, **Exarchos** persists workflow state and artifacts across sessions via an event-sourced MCP server, enforces quality verification at every phase boundary, and traces every agent decision from design requirement to merged code — all in ~2-3k tokens of context overhead.

## Tagline Candidates

Ranked by recommendation:

1. **"Your agents forget. Exarchos doesn't."** — Pain-led, memorable, conversational. Works in ads and social. **Currently used as README headline.**
2. **"Your plan.md workflow, systematized."** — Speaks directly to power users who already structure their agent work manually. High recognition factor.
3. **"Checkpoint. Rehydrate. Ship."** — Action-oriented, demonstrates the core loop. Strong for ads.
4. **"Durable SDLC workflows for Claude Code"** — Technically precise, marketplace-optimized. Best for the listing subtitle.
5. **"90% fewer tokens. 100% of the context."** — Token efficiency angle. Quantified, specific.

---

## Twitter/X Templates

### 1. Rehydrate-led (primary angle)
> Context compaction killed my Claude Code workflow mid-feature. Again. So we built /rehydrate — checkpoint any workflow, walk away, restore full awareness later in ~2-3k tokens. No history replay. Artifacts persist as references, not inlined. Free plugin. github.com/lvlup-sw/exarchos

### 2. Token efficiency angle
> Our Claude Code plugin uses 90% fewer tokens for state queries than reading full workflow state. Diff-based code review. Artifact references instead of inlining. Context economy as a quality gate. Every token saved on overhead is a token spent on actual code. github.com/lvlup-sw/exarchos

### 3. Pain-led
> Claude Code agents lose context mid-task, skip tests, and leave no trace of what they did. Exarchos adds SDLC structure — checkpoint/rehydrate across sessions, 5-dimension quality gates, full audit trail. Free plugin. github.com/lvlup-sw/exarchos

### 4. Outcome-led
> Shipped a feature with Claude Code yesterday. Exarchos structured the workflow: design → plan → delegate to 3 agent teammates → two-stage review → PR. Context died twice. /rehydrate resumed both times. ~2-3k tokens each time, not 20k.

### 5. Differentiation-led
> Most Claude Code plugins suggest good behavior. Exarchos verifies it — 5 quality dimensions checked at every phase boundary, event-sourced state that survives context loss, provenance from requirement to merged code. And it costs fewer tokens than re-explaining your project.

### 6. "You're already doing this" angle
> If you use Claude Code seriously, you probably have a plan.md workflow. Write a spec, iterate, execute, commit artifacts. It works — until context compaction hits mid-plan. Exarchos systematizes what you're already doing: design → plan → implement → review → ship. But with persistent state, quality gates, and ~2-3k tokens to resume. github.com/lvlup-sw/exarchos

### 7. HN-thread resonance (Thread 1)
> HN thread yesterday: "I start with a project.md, iterate on plan.md, then tell it to execute." Multiple devs independently building the same workflow. That's what Exarchos does — except the state persists across sessions, quality is verified at every phase, and it costs 90% fewer tokens than re-explaining your project. github.com/lvlup-sw/exarchos

### 8. Cognitive load / orchestration overhead
> "It looks cognitively like being a pilot landing a plane all day long." That's a dev describing multi-agent orchestration with tmux + markdown specs. One built a 300+ spec system with an 8-stage lifecycle — mirrors our phases almost exactly. Missing piece: persistent state and automated gates. That's what Exarchos adds. github.com/lvlup-sw/exarchos

### 9. Token burn angle
> "The parallel agents burn through tokens extremely quickly and hit Max plan limits in under an hour." The overhead isn't just cognitive — it's economic. Exarchos: field-projected queries (90% fewer tokens), diff-based review (97% savings), ~2-3k tokens to rehydrate. Structure that pays for itself. github.com/lvlup-sw/exarchos

### 10. 300-spec validation
> Someone on HN built a 300+ spec system for Claude Code — 8 lifecycle stages, slash commands, structured markdown. It works. But: no persistent state across sessions. No automated phase gates. No convergence verification. Exarchos is the systematic version of what power users are building by hand. github.com/lvlup-sw/exarchos

---

## LinkedIn Templates

### 1. Rehydrate + token efficiency angle (primary)
Here's a number that should bother you: when Claude Code loses context mid-task, re-explaining your project costs 10-20k tokens. That's tokens spent on recovery, not on writing code.

We built Exarchos — a free Claude Code plugin with an event-sourced MCP server. Checkpoint any workflow, walk away, come back later and run /rehydrate. Full workflow awareness restored in ~2-3k tokens. Design docs, plans, and PR links persist as lightweight references — never inlined into context.

The token efficiency goes deeper:
- Field-projected state queries: 90% fewer tokens than full state reads
- Diff-based code review: only changed lines, not full files
- Context economy as a verified quality gate — code too complex for LLM context can't ship

Every token saved on workflow overhead is a token spent on your actual code.

If you use Claude Code for multi-step development, this changes the economics of how you work.

### 2. Industry-trend angle
The agent coding tools debate is settled: durable execution wins. Temporal proved it for backend workflows. Now the same principle applies to AI-assisted development.

Claude Code is the most capable coding agent available, but it has no workflow structure. Agents lose context, skip verification steps, and produce code with no audit trail. Every session starts from scratch.

We built Exarchos — a free Claude Code plugin that adds SDLC structure. Checkpoint/rehydrate across sessions in ~2-3k tokens. Five-dimensional convergence gates at every phase boundary. Parallel agent teams in isolated git worktrees. Token-efficient by design — field-projected queries, diff-based review, artifact references instead of inlining.

It's the same architectural pattern (event sourcing, phase-gated transitions, adversarial verification) that enterprise systems use — packaged as a free, open-source plugin for individual developers.

### 3. "You're already doing this" angle
If you use Claude Code for real development, you've probably built your own version of structured workflows. A plan.md you iterate on before implementation. A spec file you commit alongside code. Maybe separate docs per feature.

I've seen this pattern independently invented by dozens of developers. One described it as "a living lexicon of the architecture." Another commits prompts in versioned directories organized by feature.

The instinct is right. The problem is that markdown files can't persist state across context compaction, enforce phase gates, or verify the agent followed the plan.

Exarchos systematizes what you're already doing. Design → plan → implement → review → ship, with persistent state, convergence gates, and checkpoint/rehydrate. Your artifacts — design docs, plans, review verdicts, PR links — survive as references across sessions, never inlined into context. The whole workflow layer costs ~2-3k tokens.

Your plan.md workflow, with teeth. Free and open-source.

### 4. Methodology angle
What separates structured AI-assisted development from "prompting and hoping"?

Two things: persistence and verification.

Persistence: checkpoint mid-task, rehydrate later. Artifacts survive as references. ~2-3k tokens to restore full workflow awareness, not 20k to re-explain your project.

Verification: five independent quality dimensions at every phase boundary. Does the code match the spec? Follow architectural patterns? Stay context-efficient? The last one matters — context economy is a quality gate. Code that's too complex for LLM context can't ship.

Exarchos is a free Claude Code plugin. Design, plan, delegate to agent teams, review, ship — with quality verified and state persisted at every step. Free and open-source.

### 5. Orchestration overhead angle
There's a bell curve in multi-agent development. Starts with one agent. Scales to 4-8 agents in tmux panes. Then back to one — because the orchestration overhead wasn't worth it.

One developer described it as "cognitively like being a pilot landing a plane all day long." Another spent "a ton of time enforcing Claude to use the system I put in place." A third built a 300+ spec system with an 8-stage lifecycle and still has no persistent state.

The bottleneck isn't the agents. It's keeping their context from drifting and their output verified.

Exarchos solves this with two human checkpoints (design + merge), auto-continuation between them, and convergence gates at every phase boundary. The agents work in isolated worktrees. The orchestrator manages state. You approve the design and approve the merge. Everything else flows.

Your plan.md workflow — with persistent state, quality gates, and 90% fewer tokens for overhead. Free and open-source.

---

## Google Ads

See `google-ads-campaign.md` for full campaign strategy, keyword targeting, and RSA configuration.

### Headlines (30 chars max)
1. `Agents Forget. Exarchos Won't` (30) — rehydrate
2. `Context Dies? Workflow Lives` (28) — rehydrate
3. `Checkpoint. Rehydrate. Ship.` (29) — rehydrate
4. `Stop Losing Agent Context` (25) — pain
5. `Your Plan.md, Systematized` (25) — structured workflows
6. `Durable Agent Workflows` (23) — benefit
7. `Verified AI-Written Code` (24) — benefit
8. `SDLC Structure for Claude` (25) — category
9. `Design → Plan → Ship` (19) — structured workflows
10. `90% Less Token Overhead` (23) — token efficiency
11. `5 Quality Gates Per Phase` (25) — proof
12. `2 Checkpoints. Full Audit.` (27) — proof
13. `Free Open Source Plugin` (22) — CTA
14. `Parallel Agent Teams` (20) — benefit
15. `Stop Landing Planes All Day` (28) — orchestration overhead
16. `Install Free From Marketplace` (30) — CTA

### Descriptions (90 chars max)
1. `Checkpoint mid-task, walk away, rehydrate later. Workflow resumes with full context.` (84)
2. `Workflows survive context loss. Design docs, plans, and PRs persist across sessions.` (83)
3. `State queries use 90% fewer tokens. Your context budget goes to code, not overhead.` (83)
4. `Free plugin adds phase-gated SDLC workflows and 5-dimension quality gates to Claude Code.` (89)
5. `Design, plan, delegate to agent teams, review, ship. Quality verified at every phase.` (83)
6. `Audit trail traces every agent decision from design requirement to merged code.` (77)
7. `Most plugins suggest good behavior. Exarchos verifies it. 5 quality gates per phase.` (82)
8. `Parallel agent teams in isolated worktrees. Orchestrator coordinates, teammates execute.` (86)
9. `2 checkpoints, not 8 tmux panes. Agents work in worktrees. You approve design and merge.` (87)
10. `300+ specs later, you'll wish it persisted. Event-sourced state survives every session.` (85)

---

## Usage Guidelines

### Always use (Tier 1 terms)
- Structured workflows / "your plan.md, systematized"
- Checkpoint / rehydrate
- Token-efficient
- Artifact references
- Durable workflows
- Convergence gates
- SDLC structure
- Audit trail
- Agent teams

### Avoid as lead terms
| Avoid | Use Instead |
|:------|:------------|
| CMDP / Constrained MDP | "structured workflows" or "convergence gates" |
| HSM / Hierarchical State Machine | "phase-gated workflow" |
| D1-D5 (numbered dimensions) | Name the dimensions explicitly |
| Governance (as lead term) | "structure" or "quality verification" |
| Enforcement (as lead term) | "verification" or "quality gates" |

### Tone
- Direct and technical — no hype, no superlatives
- Lead with concrete capabilities, not abstract promises
- Acknowledge trade-offs honestly (e.g., learning curve exists)
- Target: developer who has used Claude Code for 2+ weeks and felt the pain
