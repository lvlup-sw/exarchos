{
  "eligibleCount": 39,
  "note": "Boot-resolved capability welds whose lifecycle is 'active' per STALE_COVER_LIFECYCLE_POLICY -- the population validateRegistrationWelds().staleCoverEligibleCount reports, and staleCoverEligibleWelds(bootResolvedWelds()) computes directly from EVENT_ANNOTATIONS. Read from disk with fs at test time (tests/unit/events/registration-validate.test.ts); the guarded run never writes this file. Update this number by hand, in the same commit that moves it, whenever EVENT_ANNOTATIONS changes which capability registrations are active -- growth and shrink both warrant a deliberate re-pin, because this population moves only on a reviewed edit to the annotation table, not as an incidental side effect of unrelated work.",
  "lastRepin": "37 -> 39. The `gate.executed` split (#1898 item 8) added two active capability registrations: `tool.budget_exceeded` (the response-economy breach, appended by the dispatch telemetry wrapper) and `ci.check_observed` (one observed CI check, appended by the same assessment pass that appends `ci.status`). Both are eligible on the day they land because both are appended by live code and both are declared -- `tool.budget_exceeded` by a MODULE_EMISSIONS row, `ci.check_observed` by assess_stack. Neither joins the break set. Previously: 38 -> 37. `eval.judge.calibrated` was RE-TIERED off `capability` onto the new `harness` tier. Its annotation asserted an effect provider appends it and named `exarchos_view`, but every `exarchos_view` action is a read of a projection, so no action on that provider could ever have emitted it; the only append is in the evaluation harness under `tools/`, outside the governed source root. A capability weld is what makes a registration eligible, so re-tiering removes it from this population -- and with it the last member of the stale-cover break set, which is now CLOSED and whose disposition ledger is empty. Previously: 41 -> 38, when stack.restacked, turn.completed and benchmark.completed were corrected from 'active' to 'planned' because no module in the tree appends them."
}
