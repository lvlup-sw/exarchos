{
  "eligibleCount": 38,
  "note": "Boot-resolved capability welds whose lifecycle is 'active' per STALE_COVER_LIFECYCLE_POLICY -- the population validateRegistrationWelds().staleCoverEligibleCount reports, and staleCoverEligibleWelds(bootResolvedWelds()) computes directly from EVENT_ANNOTATIONS. Read from disk with fs at test time (tests/unit/events/registration-validate.test.ts); the guarded run never writes this file. Update this number by hand, in the same commit that moves it, whenever EVENT_ANNOTATIONS changes which capability registrations are active -- growth and shrink both warrant a deliberate re-pin, because this population moves only on a reviewed edit to the annotation table, not as an incidental side effect of unrelated work.",
  "lastRepin": "41 -> 38. Dispositioning the stale-cover break set found three registrations annotated 'active' that no module in the tree appends -- stack.restacked, turn.completed and benchmark.completed, each with a consumer fold written ahead of a producer that does not exist. Their lifecycle was corrected to 'planned', which is what took them out of the eligible population; the remaining 19 unnamed active welds are genuinely active and carry a row apiece in STALE_COVER_DISPOSITIONS. The exact-equality assertion did its job here: the shrink could not land without this line moving with it."
}
