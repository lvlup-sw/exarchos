# Primary Workflows

| Task | Command |
|------|---------|
| New feature/design | `/ideate` |
| Bug fix | `/debug` |
| Code improvement | `/refactor` |

Supporting commands (`/plan`, `/delegate`, `/review`, `/synthesize`, `/resume`, `/checkpoint`) are phase commands invoked within workflows — do not suggest as starting points.
