---
description: Re-inject workflow state and behavioral guidance into current context
---

# /rehydrate

Canonical alias for the Exarchos `/rehydrate` workflow command.

Invoke the `rehydrate` skill to handle: $ARGUMENTS
